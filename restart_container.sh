#!/bin/bash

# Replace "your_container_name_or_id" with the actual name or ID of your Docker container
sudo docker restart dashboard-backend

# chmod +x restart_container.sh
# crontab -e
# crontab -l
# grep CRON /var/log/syslog
